<div class="text-center">
  <h2>{{name}}</h2>

  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>

    {{!-- may want to add if loggedin to only allow this to show when logged in --}}
    <form class="form add-comment-form">
        <div class="form-group">
            <label for="project-name">Add Comment:</label>
            <input class="form-input" type="text" id="comment-text" name="comment-text" />
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
    </form>

</div>

<script src="../js/comment.js"></script>